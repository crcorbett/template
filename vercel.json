{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "bun install",
  "buildCommand": "turbo run build --filter=web && mkdir -p .vercel && cp -r apps/web/.vercel/output .vercel/",
  "framework": null
}
